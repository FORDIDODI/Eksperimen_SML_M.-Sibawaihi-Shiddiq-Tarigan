global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'diabetes_model_metrics'
    static_configs:
      - targets: ['localhost:8000']
        labels:
          service: 'diabetes-prediction'
          environment: 'production'